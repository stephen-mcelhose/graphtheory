# Learning Workspace Convention

The recommended learner state path is:

```text
{project_root}/learning/{student}/
```

Suggested files:
- `learner_state.md`
- `quiz_history.md`
- `notes.md`
- `mistakes.md`
- `chapter_progress.md`

For this repository, `project_root` is the root of `graphtheory`.
