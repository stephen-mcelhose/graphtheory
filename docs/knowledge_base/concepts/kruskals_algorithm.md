# Kruskal's Algorithm

## Definition
Kruskal's algorithm is a greedy algorithm for constructing a **minimum spanning tree** of a weighted connected graph.

## Intuition
Sort edges by weight and add the cheapest edge that does not create a cycle.

## In this project
- Code: `mst/mst.go`
- Example: `examples/algorithms/kruskal.go`
- Write-up: `docs/algorithms/kruskal.md`

## Related concepts
- depends on: weighted graph, spanning tree, minimum spanning tree, greedy algorithm
- contrasts with: Hierholzer's algorithm, Hamiltonian backtracking
- see also: Prim's algorithm
